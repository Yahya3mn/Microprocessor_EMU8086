DATA SEGMENT
    SAYILAR DB 8,5,3,6,2
    CIFT_SAYILAR_ADETI DW 0
ENDS

CODE SEGMENT
    ASSUME DS:DATA CS:CODE
START:
  
  MOV AX, DATA
  MOV DS, AX 
  
  MOV AX,0
  LEA SI,SAYILAR
  
  LOOP2:
    CMP [SI],0
    JE EVEN
    CMP [SI],1
    JE EVEN
    INC AX
    EVEN:
    INC SI
    LOOP LOOP2
    
  MOV CIFT_SAYILAR_ADETI, AX
  
  INT 20H
ENDS
END START